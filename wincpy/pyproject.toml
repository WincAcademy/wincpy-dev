[build-system]
requires = ["setuptools>=80", "setuptools-scm>=8"]
build-backend = "setuptools.build_meta"

[project]
name = "wincpy"
dynamic = ["version"]

[tool.setuptools_scm]
version_file = "wincpy/_version.py"
